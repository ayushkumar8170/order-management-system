# Local development defaults. In Docker/production these are overridden by
# the DB_HOST / DB_PORT / DB_NAME / DB_USER / DB_PASSWORD environment
# variables set in docker-compose.yml — see config.DatabaseConfig.
db.host=localhost
db.port=3306
db.name=oms_db
db.user=oms_user
db.password=oms_password
